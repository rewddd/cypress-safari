{
  "devDependencies": {
    "cypress": "^8.6.0",
    "playwright": "^1.15.2"
  },
  "scripts": {
    "cy:open": "npx cypress open",
    "cy:install": "npx cypress install",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:ff": "npx cypress run --browser firefox",
    "test": "jest --coverage",
    "lint": "eslint src/{,**/}*.ts* --cache",
    "lint:fix": "yarn lint --fix"
  }
}
